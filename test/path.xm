system = Module("system"
os = Module("os"
print = system.out.write_line

path = os.Path(system.script) / ".."
print(path
(path / "path.xm").__string() == system.script || throw Throwable("must equal."
(os.Path(".") / "..").__string() == os.Path("..").__string() || throw Throwable("must equal."
