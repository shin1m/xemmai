assert = @(x) x || throw Throwable("Assertion failed."

if false
	a = b = c = d = e = f = 0
else
	assert(a === null
	while true
		assert(b === null
		break
	assert(b === null
	for i = 0; i < 1; i = i + 1
		assert(c === null
		c = 0
	try
		throw d
	catch Throwable t
	catch Null n
		assert(n === null
	finally
		assert(e === null
		assert(t === null
assert(f === null
