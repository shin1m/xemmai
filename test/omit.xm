system = Module("system"
print = system.error.write_line
assert = @(x) x || throw Throwable("Assertion failed."

xs = ["foo", "bar"

assert(null !== xs.('size
assert(true === xs.?('size
assert(2 === xs.size(

true && (x = xs[0
assert("foo" == x
